import { hVariable } from 'crumbs/helpers/h-variable';
import { module, test } from 'qunit';

module('Unit | Helper | h variable');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = hVariable([42]);
  assert.ok(result);
});
