import { hIsRouteDetails } from 'crumbs/helpers/h-is-route-details';
import { module, test } from 'qunit';

module('Unit | Helper | h is route details');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = hIsRouteDetails([42]);
  assert.ok(result);
});
