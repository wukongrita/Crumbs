import { hLookupProperties } from 'crumbs/helpers/h-lookup-properties';
import { module, test } from 'qunit';

module('Unit | Helper | h lookup properties');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = hLookupProperties([42]);
  assert.ok(result);
});
